import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cs.pull.to.refresh',
  appName: 'cs-pull-to-refresh',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
